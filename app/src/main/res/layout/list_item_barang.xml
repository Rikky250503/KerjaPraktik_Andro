<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_margin="6dp"
    android:elevation="32dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/box_nama_barang"
        android:layout_width="match_parent"
        android:layout_height="130dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginHorizontal="10dp"
        android:layout_marginTop="10dp"
        android:background="@drawable/img_3">
        <TextView
            android:id="@+id/tv_id_barang_tampilan"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="id"
            android:textSize="21sp"
            android:visibility="gone"
            app:layout_constraintTop_toTopOf="@+id/box_nama_barang"
            />

        <TextView
            android:id="@+id/tv_nama_barang_tampilan"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="PT.Cahaya Makmur"
            android:textStyle="bold"
            android:textSize="24sp"
            android:textColor="#4A4545"
            android:layout_marginTop="30dp"
            android:layout_marginHorizontal="20dp"
            app:layout_constraintTop_toTopOf="@id/box_nama_barang"
            app:layout_constraintStart_toStartOf="@id/box_nama_barang"
            />
    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>